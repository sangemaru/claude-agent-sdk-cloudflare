{
  "name": "claude-agent-worker",
  "version": "1.0.0",
  "description": "Claude Agent SDK on Cloudflare Containers",
  "type": "module",
  "main": "server.ts",
  "scripts": {
    "prebuild": "npx tsx scripts/bundle-framework.ts",
    "dev": "npm run prebuild && npx wrangler dev",
    "deploy": "npm run prebuild && npx wrangler deploy",
    "sync-skills": "npx tsx scripts/sync-skills.ts",
    "sync-agents": "npx tsx scripts/sync-agents.ts",
    "sync-framework": "npx tsx scripts/sync-framework.ts",
    "sync-all": "npm run sync-skills && npm run sync-agents && npm run sync-framework"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.28",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250110.0",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.2"
  }
}
